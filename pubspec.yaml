name: labib_admin_panel
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  url_launcher: ^6.2.5
  flutter:
    sdk: flutter
  http: ^1.1.0

  cupertino_icons: ^1.0.8
  go_router: ^14.8.1
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true